# 安全说明

## 报告漏洞

请不要在公开 Issue 中披露可利用的安全漏洞。请通过 GitHub 仓库所有者的私密联系方式提交复现步骤、影响范围和建议修复方式。

## 部署提醒

- `config.example.ini`、SQL 中的账号和密码仅供本地演示，不应直接用于生产环境。
- 不要将 MySQL 3306 端口直接暴露到公网；应限制来源地址或使用可信内网。
- `sql/00_setup.sql` 会删除并重建数据库，只能在明确的本地演示环境执行。
- 当前密码摘要使用 SHA-256，适合作业演示但不适合生产认证；生产系统应使用 Argon2、scrypt 或 bcrypt，并为每个密码生成独立盐值。
