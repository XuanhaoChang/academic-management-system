# 数据库21表键说明（主键/外键/唯一键/索引业务意义）

说明：
- 本文按当前项目 SQL 设计整理，覆盖 21 张核心表。
- “键”包含主键、外键、唯一键及关键索引（用于查询性能与并发一致性）。
- 表按业务域分组，便于答辩时按模块讲解。

## 一、系统与权限域（8表）

### 1) sys_roles（角色表）
- 主键：PRIMARY KEY (id)
  - 意义：角色唯一标识，供 sys_users.role_id 与权限映射引用。
- 唯一键：UNIQUE(role_name)
  - 意义：防止角色名称重复（如两个“管理员”）。
- 唯一键：UNIQUE(role_code)
  - 意义：保证程序内部角色编码稳定唯一（如 ADMIN/TEACHER/STUDENT）。

### 2) sys_permissions（权限表）
- 主键：PRIMARY KEY (id)
  - 意义：权限项唯一标识。
- 唯一键：UNIQUE(permission_code)
  - 意义：避免同一权限编码重复，便于后端做权限判断。

### 3) sys_role_permissions（角色-权限关联表）
- 主键：PRIMARY KEY (id)
  - 意义：关联记录主键。
- 唯一键：uk_role_permission(role_id, permission_id)
  - 意义：防止同一角色重复绑定同一权限。
- 外键：fk_role_perm_role(role_id) -> sys_roles(id)
  - 意义：保证 role_id 一定存在于角色表。
- 外键：fk_role_perm_perm(permission_id) -> sys_permissions(id)
  - 意义：保证 permission_id 一定存在于权限表。

### 4) base_dept（院系表）
- 主键：PRIMARY KEY (id)
  - 意义：院系唯一标识。
- 唯一键：UNIQUE(dept_code)
  - 意义：院系编码唯一，便于对接其他系统或导入数据。

### 5) base_major（专业表）
- 主键：PRIMARY KEY (id)
  - 意义：专业唯一标识。
- 唯一键：UNIQUE(major_code)
  - 意义：专业编码唯一，避免同码不同名冲突。
- 外键：fk_major_dept(dept_id) -> base_dept(id)
  - 意义：专业必须隶属于有效院系。

### 6) sys_users（用户表）
- 主键：PRIMARY KEY (id)
  - 意义：系统用户唯一标识，多个业务表都依赖该键。
- 唯一键：UNIQUE(username)
  - 意义：登录账号唯一。
- 外键：fk_user_role(role_id) -> sys_roles(id)
  - 意义：用户必须绑定合法角色。
- 外键：fk_user_dept(dept_id) -> base_dept(id)
  - 意义：用户可关联所属院系。
- 外键：fk_user_major(major_id) -> base_major(id)
  - 意义：学生可关联所属专业。

### 7) sys_audit_logs（审计日志表）
- 主键：PRIMARY KEY (id BIGINT)
  - 意义：日志高频写入，使用 BIGINT 提供更大容量。
- 外键：fk_audit_user(user_id) -> sys_users(id)
  - 意义：记录“谁触发了操作”。
- 索引：idx_audit_created_at(created_at)
  - 意义：支持按时间范围快速检索日志（答辩常问“如何查最近操作”）。

### 8) sys_config（系统配置表）
- 主键：PRIMARY KEY (id)
  - 意义：配置项记录主键。
- 唯一键：UNIQUE(config_key)
  - 意义：每个配置键仅一条有效记录（如 current_semester）。

## 二、教学与成绩域（6表）

### 9) edu_courses（课程表）
- 主键：PRIMARY KEY (id)
  - 意义：课程唯一标识。
- 唯一键：UNIQUE(course_code)
  - 意义：课程编码全局唯一（如 MA201）。
- 外键：fk_course_dept(dept_id) -> base_dept(id)
  - 意义：课程归属院系一致性。

### 10) edu_teaching（授课安排表）
- 主键：PRIMARY KEY (id)
  - 意义：教学班实例主键（不是课程主键）。
- 外键：fk_teaching_course(course_id) -> edu_courses(id)
  - 意义：每个教学班对应一门课程。
- 外键：fk_teaching_teacher(teacher_id) -> sys_users(id)
  - 意义：每个教学班对应一位教师。
- 检查约束：ck_teaching_max_count_pos(max_count > 0)
  - 意义：保证容量设置合法。
- 关键字段语义：status, enrolled_count, is_submitted
  - 意义：支撑“选课状态控制”和“成绩提交后锁定”。

### 11) edu_grades（成绩表）
- 主键：PRIMARY KEY (id)
  - 意义：成绩记录唯一标识。
- 唯一键：uk_student_teaching(student_id, teaching_id)
  - 意义：同一学生在同一教学班只能有一条成绩记录。
- 外键：fk_grade_student(student_id) -> sys_users(id)
  - 意义：成绩归属到有效学生。
- 外键：fk_grade_teaching(teaching_id) -> edu_teaching(id)
  - 意义：成绩归属到有效教学班。
- 索引：idx_grades_teaching_del_student(teaching_id, is_deleted, student_id)
  - 意义：优化班级成绩查询、成绩提交校验与统计。
- 检查约束：ck_grade_exam_score_range, ck_grade_score_range
  - 意义：保证卷面分/总评在 0~100。

### 12) edu_archives（历史成绩归档表）
- 主键：PRIMARY KEY (id)
  - 意义：归档记录主键。
- 业务键：original_grade_id
  - 意义：指向原成绩记录来源，防止重复归档、支持追溯。
- 说明：当前脚本未显式添加外键到 edu_grades
  - 意义：归档后主表会软删除，弱耦合可降低归档链路阻塞风险。

### 13) edu_daily_records（平时记录表）
- 主键：PRIMARY KEY (id)
  - 意义：平时记录主键。
- 唯一键：uk_dr(teaching_id, student_id, record_date, record_type)
  - 意义：同一学生同一天同类型记录不能重复。
- 外键：fk_dr_teaching(teaching_id) -> edu_teaching(id)
  - 意义：记录必须归属于有效教学班。
- 外键：fk_dr_student(student_id) -> sys_users(id)
  - 意义：记录必须归属于有效学生。
- 索引：idx_dr_teaching(teaching_id)
  - 意义：按班级聚合平时分时加速。

### 14) edu_schedule_change_req（调课申请表）
- 主键：PRIMARY KEY (id)
  - 意义：调课申请唯一标识。
- 外键：fk_req_teaching(teaching_id) -> edu_teaching(id)
  - 意义：申请必须对应真实教学班。
- 外键：fk_req_teacher(teacher_id) -> sys_users(id)
  - 意义：申请必须由真实教师发起。
- 索引：idx_req_teacher(teacher_id)
  - 意义：教师查看“我的申请”加速。
- 索引：idx_req_status(status)
  - 意义：管理员按状态审批加速。
- 复合索引：idx_req_status_del_created(status, is_deleted, created_at)
  - 意义：优化“待审批列表”按状态+未删+时间排序场景。

## 三、学生业务域（7表）

### 15) stu_info（学生扩展信息表）
- 主键：PRIMARY KEY (id)
  - 意义：扩展信息记录主键。
- 唯一键：UNIQUE(student_id)
  - 意义：每个学生仅一条扩展档案。
- 外键：fk_stu_info_user(student_id) -> sys_users(id)
  - 意义：扩展档案必须绑定有效学生账号。

### 16) stu_selection（选课记录表）
- 主键：PRIMARY KEY (id)
  - 意义：选课记录主键。
- 唯一键（最终语义）：(student_id, teaching_id, deleted_at)
  - 意义：同一学生对同一教学班“同一有效状态”只保留一条记录；deleted_at=0 代表当前有效选课。
- 外键：fk_stu_selection_student(student_id) -> sys_users(id)
  - 意义：选课记录必须对应合法学生。
- 外键：fk_stu_selection_teaching(teaching_id) -> edu_teaching(id)
  - 意义：选课记录必须对应合法教学班。
- 复合索引：idx_sel_teaching_del_student(teaching_id, deleted_at, student_id)
  - 意义：优化按教学班统计在选人数与名单查询，是并发选课流程关键索引。

### 17) stu_waiting_list（候补队列表）
- 主键：PRIMARY KEY (id)
  - 意义：候补记录主键。
- 唯一键（最终语义）：(student_id, teaching_id, deleted_at)
  - 意义：防止同一学生重复进入同一课程候补。
- 外键：fk_waiting_student(student_id) -> sys_users(id)
  - 意义：候补记录必须对应合法学生。
- 外键：fk_waiting_teaching(teaching_id) -> edu_teaching(id)
  - 意义：候补记录必须对应合法教学班。
- 复合索引：idx_wait_teaching_del_queue(teaching_id, deleted_at, queue_no, id)
  - 意义：优化候补按队列顺序出队（递补场景）。

### 18) stu_course_prereq（课程先修关系表）
- 主键：PRIMARY KEY (id)
  - 意义：先修关系记录主键。
- 唯一键：uk_course_prereq(course_id, pre_course_id)
  - 意义：避免同一先修关系重复录入。
- 外键：fk_pr_course(course_id) -> edu_courses(id)
  - 意义：主课程必须合法。
- 外键：fk_pr_pre_course(pre_course_id) -> edu_courses(id)
  - 意义：先修课程必须合法。

### 19) stu_exam_schedule（考试安排表）
- 主键：PRIMARY KEY (id)
  - 意义：考试安排记录主键。
- 唯一键：uk_exam(course_id, semester)
  - 意义：同一课程同一学期只允许一套考试安排。
- 外键：fk_exam_course(course_id) -> edu_courses(id)
  - 意义：考试安排必须对应有效课程。

### 20) stu_exam_defer_req（缓考申请表）
- 主键：PRIMARY KEY (id)
  - 意义：缓考申请主键。
- 唯一键：uk_exam_defer(student_id, course_id, semester)
  - 意义：同一学生对同一课程同学期仅允许一条申请。
- 外键：fk_defer_student(student_id) -> sys_users(id)
  - 意义：申请人必须是有效学生。
- 外键：fk_defer_course(course_id) -> edu_courses(id)
  - 意义：申请课程必须有效。

### 21) stu_evaluation（课程评教表）
- 主键：PRIMARY KEY (id)
  - 意义：评教记录主键。
- 唯一键：uk_eval(student_id, course_id, semester)
  - 意义：同一学生同一课程同学期只评一次，防止刷分。
- 外键：fk_eval_student(student_id) -> sys_users(id)
  - 意义：评教人必须是有效学生。
- 外键：fk_eval_course(course_id) -> edu_courses(id)
  - 意义：被评课程必须有效。

## 四、答辩建议：如何讲“键设计”的价值

- 从“防错”角度讲：唯一键防重复数据，外键防孤儿数据，检查约束防非法值。
- 从“性能”角度讲：复合索引对应高频 where 条件与排序字段，避免全表扫描。
- 从“一致性”角度讲：并发选课依赖唯一键和索引，归档依赖业务键 original_grade_id 防重。
- 从“可追溯”角度讲：软删除 + 审计日志让系统具备回溯与问责能力。
