# 数据库触发器与答辩要点

## 一、触发器总览（当前项目共4个）

1) trigger_audit_log_sys_users_insert
- 所在脚本：my_db_project/sql/04_triggers.sql
- 类型：AFTER INSERT ON sys_users
- 作用：用户新建后自动写审计日志（action_type='USER_CREATED'）。

2) trigger_audit_log_sys_users_update
- 所在脚本：my_db_project/sql/04_triggers.sql
- 类型：AFTER UPDATE ON sys_users
- 作用：用户信息修改后自动写审计日志（action_type='USER_UPDATED'），记录 username 和 is_deleted。

3) trg_after_grade_submit
- 所在脚本：my_db_project/sql/06_teacher_ext.sql
- 类型：AFTER UPDATE ON edu_teaching
- 触发条件：NEW.is_submitted = 1 且 OLD.is_submitted = 0
- 作用：成绩提交锁定时写审计日志（action_type='SUBMIT_GRADES'）。

4) trigger_auto_fill_vacancy
- 所在脚本：my_db_project/sql/08_student_ext.sql
- 类型：AFTER UPDATE ON stu_selection
- 触发条件：NEW.deleted_at <> 0 且 OLD.deleted_at = 0（即退课软删除）
- 作用：同步 edu_teaching.enrolled_count 与 status='OPEN'。

## 二、逐个触发器详解

### 1. trigger_audit_log_sys_users_insert
触发时机：
- 新增 sys_users 记录后立即触发。

写入内容：
- user_id = NEW.id
- action_type = 'USER_CREATED'
- detail = username

业务意义：
- 保证“新增账号”行为可追溯，避免管理员误操作无法定位。
- 审计逻辑下沉数据库层，避免应用层漏记。

可能被问：为什么用 AFTER 而不是 BEFORE？
- 因为日志需要稳定引用已成功写入的 user_id，AFTER 更稳妥。

### 2. trigger_audit_log_sys_users_update
触发时机：
- 任意更新 sys_users 后触发。

写入内容：
- user_id = NEW.id
- action_type = 'USER_UPDATED'
- detail 含 username 和 is_deleted

业务意义：
- 对“改名、软删除、恢复”等用户关键变更形成留痕。

风险与控制：
- 风险：高频更新可能导致日志增长快。
- 控制：sys_audit_logs.created_at 已建索引，支持时间范围检索和归档策略。

### 3. trg_after_grade_submit
触发时机：
- edu_teaching 更新后，且 is_submitted 从 0 -> 1。

写入内容：
- user_id = NEW.teacher_id
- action_type = 'SUBMIT_GRADES'
- detail 含 teaching_id 和 semester

业务意义：
- 成绩提交后需要强审计，确保“谁在何时提交了哪个教学班”可追踪。
- 与 proc_submit_grades 形成配合：过程负责校验与锁定，触发器负责留痕。

为什么要加 OLD/NEW 条件判断？
- 避免重复写日志：仅首次提交时记录一次，后续无关更新不重复记录。

### 4. trigger_auto_fill_vacancy
触发时机：
- stu_selection 更新后，检测到退课（deleted_at 从0变为非0）。

执行逻辑：
- 对应教学班 enrolled_count 减1（最低到0）；
- status 置为 OPEN。

业务意义：
- 保证退课后“可选容量状态”及时同步。
- 与服务层分工明确：触发器只做轻量同步，候补递补由服务层事务处理。

为什么不在触发器里直接做递补？
- 触发器内再写同类业务表容易引发锁链复杂化和死锁风险。
- 当前方案把复杂分支移到应用事务，更易控制与调试。

## 三、触发器相关验证SQL（答辩可现场演示）

### 1) 验证用户新增审计
```sql
INSERT INTO sys_users(username, real_name, role_id, password_hash)
VALUES('demo_user_x', '演示用户', 3, SHA2('123456', 256));

SELECT * FROM sys_audit_logs
WHERE action_type='USER_CREATED'
ORDER BY created_at DESC
LIMIT 5;
```

### 2) 验证用户更新审计
```sql
UPDATE sys_users
SET real_name='演示用户-已改'
WHERE username='demo_user_x' AND is_deleted=0;

SELECT * FROM sys_audit_logs
WHERE action_type='USER_UPDATED'
ORDER BY created_at DESC
LIMIT 5;
```

### 3) 验证成绩提交审计
```sql
CALL proc_submit_grades(1);

SELECT * FROM sys_audit_logs
WHERE action_type='SUBMIT_GRADES'
ORDER BY created_at DESC
LIMIT 5;
```

### 4) 验证退课后人数同步
```sql
-- 假设记录id=100是有效选课记录
UPDATE stu_selection
SET deleted_at = UNIX_TIMESTAMP(NOW(3))*1000, is_deleted = 1
WHERE id = 100 AND deleted_at = 0;

SELECT id, enrolled_count, status
FROM edu_teaching
WHERE id = (SELECT teaching_id FROM stu_selection WHERE id=100);
```

## 四、和触发器紧密相关的答辩高频问题

Q1：你们如何避免“业务代码忘记写日志”？
- A：关键留痕放在数据库触发器层，任何入口（UI/脚本/管理端）修改都会被统一记录。

Q2：触发器会不会拖慢系统？
- A：我们只在关键表和关键动作上使用轻量触发器，日志表有时间索引；重业务逻辑（如候补递补）放服务层事务，不在触发器内扩散。

Q3：为什么“退课递补”不全放数据库触发器？
- A：递补涉及队列、并发和多表更新，放触发器容易产生死锁和排查困难；我们采用“触发器做人数同步 + 服务层事务做递补”的折中设计。

Q4：如何保证成绩提交后不能再改？
- A：在 edu_teaching 增加 is_submitted 锁定标志；提交动作必须走 proc_submit_grades（先校验完整性，再置锁），UI 与服务层根据该标志转只读。

Q5：归档后如何追溯原数据？
- A：edu_archives 存 original_grade_id，并保留 student_id、teaching_id、score、archived_at，可按学期和教学班追溯。

Q6：你们如何应对并发选课超卖？
- A：proc_attempt_enroll 使用 FOR UPDATE 锁定教学班，实时 count + 容量判断后写入，配合唯一约束和索引防止重复与脏写。

## 五、建议你们答辩时的讲解顺序（30-60秒）

1) 先讲“为什么要触发器”：审计自动化、减少漏记。
2) 再讲“我们只做轻触发器”：不把复杂递补硬塞数据库。
3) 然后举一个完整链路：成绩提交 -> is_submitted=1 -> 触发器写审计日志。
4) 最后补一句：并发与一致性由“存储过程 + 事务 + 约束 + 索引”共同保障。
