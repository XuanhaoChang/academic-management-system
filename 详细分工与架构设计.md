# 数据库课程设计：详细分工与架构规约 (High Performance Edition)

## 0. 设计目标与高分策略
为了确保拿到优异成绩，本项目在标准增删改查（CRUD）基础上，引入以下**高工作量**与**技术深度**特性：
1. **全栈式数据库应用**：深度使用 存储过程 (Stored Procedures)、触发器 (Triggers)、视图 (Views) 和 事务 (Transactions)。
2. **高并发与一致性**：模拟选课时的并发控制（使用数据库锁或事务隔离级别）。
3. **数据安全与审计**：实现列级权限控制、敏感数据加密（MD5/SHA）、操作审计日志。
4. **可视化与大数据接口**：不仅是表格，还包括基于 SQL 窗口函数的数据透视与图表展示。
5. **软删除与数据恢复**：所有“删除”操作均为逻辑删除，支持数据回滚。

---

## 1. 系统架构与技术栈
*   **开发语言**: Python 3.10+
*   **GUI 框架**: PyQt6 / PySide6 (支持 QSS 样式表)
*   **数据库**: MySQL 8.0+
*   **架构模式**: MVC (Model-View-Controller) 分层架构
    *   `Presentation Layer (View)`: Qt `.ui` 文件与视图类
    *   `Business Logic Layer (Service)`: Python 业务逻辑处理
    *   `Data Access Layer (DAO)`: 封装 SQL 语句，直接与 DB 交互

---

## 2. 数据库设计规范 (Database Schema Strategy)
为了避免合并冲突，每位成员拥有自己“主控”的表，对其他人的表仅只读。

| 成员 | 负责表 (主控权限：建表、修改结构) | SQL 重点展示 |
| :--- | :--- | :--- |
| **组长** | `sys_users` (用户), `sys_roles` (角色), `sys_permissions`, `sys_audit_logs` (审计), `base_dept` (院系), `base_major` (专业), `sys_config` (系统配置) | DCL (用户权限), 备份恢复脚本 |
| **组员 A** | `edu_courses` (课程基础), `edu_teaching` (授课安排), `edu_grades` (成绩), `edu_archives` (历史归档) | Window Functions, Complex Views, Aggregation |
| **组员 B** | `stu_info` (学生信息), `stu_selection` (选课记录), `stu_waiting_list` (候补队列), `stu_evaluation` (评教), `stu_exam_schedule` (考试安排), `stu_exam_defer_req` (缓考申请) | Transactions, Triggers, Stored Procedures |

---

## 3. 详细分工与接口定义 (Interface Specification)

### 🦁 1. 组长 (34%) - 架构核心与安全中枢

**核心任务**：搭建不仅能跑，而且“稳”的系统框架。
*   **拓展功能建议**：
    *   **数据库连接池 (Connection Pooling)**：手写或集成连接池，防止连接泄露。
    *   **自动备份系统**：调用 `mysqldump` 进行定时全量备份。
    *   **防 SQL 注入封装**：封装一个底层 `DBHelper` 类，强制使用参数化查询。

**文件与接口设计**：

| 模块 | 文件路径 | 类名/接口名 | 接口描述 |
| :--- | :--- | :--- | :--- |
| **基础设施** | `core/db_manager.py` | `DBManager` | `verify_connection()`: 检查连通性<br>`exec_query(sql, params)`: 通用查询<br>`exec_transaction(tasks)`: 事务执行器 |
| **认证服务** | `services/auth_service.py` | `AuthService` | `login(username, password_hash)`: 校验并返回 User 对象<br>`update_password(uid, old, new)`: 修改密码 |
| **主窗口** | `views/main_window.py` | `MainWindow` | `init_navigation(role)`: 根据角色动态加载菜单 (RBAC核心)<br>`switch_page(page_idx)`: 页面路由 |
| **审计系统** | `core/logger.py` | `AuditLogger` | `log_action(user_id, action_type, detail)`: 写入 `sys_audit_logs` 表 |
| **管理员UI** | `views/admin/user_mgmt.py` | `UserMgmtWidget` | `import_users_from_csv(file_path)`: **批量导入功能**<br>`soft_delete_user(user_id)`: 软删除接口<br>✅ **[已实现]** 按钮重组与UI优化(Task 1) |

**SQL 任务**：
*   `init_schema.sql`: 负责全库初始化脚本的合并。
*   `trigger_audit_log`: 编写触发器，当重要表数据变动时，自动向日志表插入记录。

---

### 🐯 2. 组员 A (33%) — 教师工作台（✅ 全部已实现）

> **数据初始化脚本**：`teacher_scripts/setup_teacher_data.py`（整合脚本，一键运行）
> **SQL 扩展**：`sql/06_teacher_ext.sql`（建表/视图/存储过程）、`sql/07_teacher_demo_data.sql`（演示数据）

---

#### 2.1 数据库扩展（`sql/06_teacher_ext.sql`）

| 对象类型 | 名称 | 说明 |
| :--- | :--- | :--- |
| 新字段 | `edu_teaching.is_submitted` | `TINYINT DEFAULT 0` — 成绩提交锁定标志（1=已锁定/归档） |
| 新字段 | `edu_grades.exam_score` | `DECIMAL(5,2)` — 卷面成绩（0-100）；最终成绩 = exam_score×0.7 + daily_score |
| 新表 | `edu_daily_records` | 平时记录（签到/作业/章节测验），UNIQUE KEY (teaching_id, student_id, record_date, record_type) |
| 新表 | `edu_schedule_change_req` | 调课申请，状态 ENUM('PENDING','APPROVED','REJECTED')，管理员审批预留接口 |
| 新视图 | `view_teacher_schedule` | 教师课表视图（JOIN edu_teaching + edu_courses + sys_users） |
| 新视图 | `view_daily_score` | 平时分汇总（SELECT teaching_id, student_id, 完成率, daily_score=30×完成率） |
| 存储过程 | `proc_submit_grades(p_teaching_id)` | 校验所有学生已有成绩后设置 is_submitted=1，否则 SIGNAL SQLSTATE '45000' |
| 触发器 | `trg_after_grade_submit` | AFTER UPDATE ON edu_teaching，is_submitted 变为1时写入审计日志 |

---

#### 2.2 服务层接口

##### `services/grade_service.py` — `GradeService`

| 方法 | 签名 | 说明 |
| :--- | :--- | :--- |
| `get_my_teachings` | `(teacher_id: int) → list[dict]` | 返回教师所有授课（含 `is_submitted` 字段，供前端判断锁定状态） |
| `batch_enter_exam_scores` | `(teaching_id, exam_dict: dict[int,float]) → int` | 批量录入卷面成绩，自动从 view_daily_score 取平时分，计算最终成绩后 UPSERT |
| `get_teaching_grades_with_exam` | `(teaching_id) → list[dict]` | 返回班级所有学生成绩（含 exam_score, daily_score, score, gpa, grade_letter） |
| `archive_semester_grades` | `(semester: str) → int` | 调用存储过程归档指定学期成绩到 edu_archives |

##### `services/teacher_service.py` — `TeacherService`（✅ 新增文件）

| 方法 | 签名 | 说明 |
| :--- | :--- | :--- |
| `get_profile` | `(teacher_id) → dict` | 获取教师个人信息（用于个人信息面板） |
| `get_my_schedule` | `(teacher_id) → list[dict]` | 从 view_teacher_schedule 获取课表，含 timeslot 字段 |
| `get_available_semesters_for_teacher` | `(teacher_id) → list[str]` | 返回教师有课的学期列表（降序） |
| `is_grades_submitted` | `(teaching_id) → bool` | 查询 edu_teaching.is_submitted |
| `submit_grades` | `(teaching_id, teacher_id) → None` | 调用 proc_submit_grades，捕获 DatabaseError → ValidationError |
| `get_class_roster` | `(teaching_id) → list[dict]` | 获取选课学生名单（先查 stu_selection，无则回退 edu_grades） |
| `get_daily_records` | `(teaching_id, record_type, record_date) → list[dict]` | 查询指定日期/类型的平时记录 |
| `batch_save_daily_records` | `(teaching_id, records: list[dict]) → int` | 批量 UPSERT edu_daily_records |
| `get_available_archive_semesters` | `(teacher_id) → list[str]` | 查询 edu_archives 中该教师有数据的学期 |
| `get_archived_grades` | `(teacher_id, semester) → list[dict]` | 查询历史归档成绩 |
| `import_grades_from_excel` | `(teaching_id, file_path) → int` | openpyxl 解析 Excel（需含”学号”/”卷面成绩”列），调用 batch_enter_exam_scores |
| `submit_reschedule_request` | `(teaching_id, teacher_id, reason, original_time, requested_time) → int` | 写入 edu_schedule_change_req，返回新记录 id |
| `list_my_reschedule_requests` | `(teacher_id) → list[dict]` | 查询该教师所有调课申请（含 course_name, semester, status, admin_comment） |

##### `models/analysis_dao.py` — `AnalysisDAO`（新增方法）

| 方法 | 签名 | 说明 |
| :--- | :--- | :--- |
| `get_daily_score_summary` | `(teaching_id) → list[dict]` | 从 view_daily_score JOIN sys_users，返回每学生的完成率/平时分 |
| `get_archive_by_teacher` | `(teacher_id, semester) → list[dict]` | 查询 edu_archives，关联课程/教师/学生信息 |

---

#### 2.3 UI 层（`ui/teacher/`）

| 文件 | 类名 | 功能说明 |
| :--- | :--- | :--- |
| `profile_panel.py` | `ProfilePanel` | 个人信息展示 + 修改密码（调用 AuthService.update_password） |
| `schedule_panel.py` | `SchedulePanel` | **Tab0** 可视化课表（QTableWidget 周历网格，按课程着色）<br>**Tab1** 调课申请（下拉选周次/节次/星期 + 快捷原因标签 + 历史申请列表） |
| `course_panel.py` | `CoursePanel` | 教师工作台主体，7个子Tab（见下表） |

**CoursePanel 子Tab 详细说明**：

| Tab | 名称 | 核心功能 | 锁定后是否可用 |
| :---: | :--- | :--- | :---: |
| 0 | 成绩录入 | 卷面成绩录入（可编辑）+ 平时分自动/手动填充 + Excel 导入 + 提交锁定 | 只读 |
| 1 | 成绩分析 | matplotlib 柱状图 + 正态分布拟合 + 6项统计卡片 + 刷新按钮 | ✅ 可查看 |
| 2 | 挂科预警 | 不及格学生列表 + 导出 Excel | ✅ 可查看 |
| 3 | 平时分管理 | 平时分汇总表（可编辑已完成数）+ 一键导入到成绩录入 | 只读 |
| 4 | 历史归档 | 按学期查询 edu_archives 归档记录 | ✅ 可查看 |
| 5 | 批量操作 | 批量调分（加/减/缩放/开方）+ 智能曲线调分 + 按条件筛选（弹窗列表） | ❌ 禁用 |
| 6 | 课程设置 | 自动保存开关 + 数据备份/恢复 | ❌ 禁用 |

---

#### 2.4 为组长预留的管理员端接口（待实现）

以下接口已在数据库侧完整实现，**组长只需在管理员UI调用即可**：

```python
# 查询所有待审批的调课申请
# 表：edu_schedule_change_req，WHERE status='PENDING'
# 建议在 services/admin_service.py 中添加：
def list_pending_reschedule_requests() -> list[dict]:
    “””返回字段: req_id, course_name, semester, teacher_name,
                 original_time, requested_time, reason, created_at”””

def process_reschedule_request(req_id: int, action: str, comment: str = “”) -> None:
    “””action: 'APPROVED' 或 'REJECTED'
       UPDATE edu_schedule_change_req SET status=%s, admin_comment=%s,
              processed_by=%s WHERE id=%s”””
```

相关表结构：
```sql
-- edu_schedule_change_req
id, teaching_id, teacher_id,
reason VARCHAR(500),
original_time VARCHAR(100),   -- 原上课时间
requested_time VARCHAR(100),  -- 申请调整到（格式: “第3教学周 周四5-6节”）
status ENUM('PENDING','APPROVED','REJECTED') DEFAULT 'PENDING',
admin_comment VARCHAR(300),
processed_by INT NULL,        -- 处理人 id
created_at DATETIME
```

---

#### 2.5 数据初始化脚本说明

**SQL 脚本执行顺序**（在组长的总初始化流程中追加）：
```
sql/06_teacher_ext.sql       # 教师端扩展建表/视图/存储过程（依赖01~05完成）
sql/07_teacher_demo_data.sql # 演示用学生 + 2024-2 学期成绩
```

**文件目录结构**（所有教师端脚本统一放在 `teacher_scripts/`）：
```
teacher_scripts/
  setup_teacher_data.py       ← 整合入口，日常使用此脚本
  load_demo_data.py           ← 加载 07_teacher_demo_data.sql 演示数据
  setup_procedures.py         ← 单独创建 proc_submit_grades 存储过程
  check_duplicate_courses.py  ← 调试用：检查重复课程记录
  query_courses.py            ← 调试用：查询教师课程列表
  add_2024_1_grades.py        ← （已整合备份）
  archive_2024_1.py           ← （已整合备份）
  create_2024_1_archive.py    ← （已整合备份）
  generate_daily_records.py   ← （已整合备份）

init_database.py              ← 根目录保留，全局数据库初始化（组长维护）
```

**Python 脚本使用方法**（在项目根目录下执行）：

```bash
# 全部步骤一键执行（推荐首次搭建演示环境时使用）
python teacher_scripts/setup_teacher_data.py

# 单步执行（按需选用）
python teacher_scripts/setup_teacher_data.py --step 1   # 写入 2024-1 归档数据并锁定课程
python teacher_scripts/setup_teacher_data.py --step 2   # 为 2024-2 生成平时记录
python teacher_scripts/setup_teacher_data.py --step 3   # 为 2024-1 补录 edu_grades（可选）
python teacher_scripts/setup_teacher_data.py --step 4   # 将 edu_grades 归档并软删除

# 其他辅助脚本
python teacher_scripts/load_demo_data.py                # 单独加载演示数据 SQL
python teacher_scripts/setup_procedures.py              # 单独重建存储过程
python teacher_scripts/query_courses.py                 # 调试：查询课程列表
python teacher_scripts/check_duplicate_courses.py       # 调试：检查重复记录
```

| 步骤 | 脚本参数 | 用途 | 演示功能 |
| :---: | :--- | :--- | :--- |
| 1 | `--step 1` | 写入 2024-1 归档数据 + 锁定课程 | “历史归档”标签、CLOSED 状态 |
| 2 | `--step 2` | 生成 2024-2 平时记录 | “平时分管理”、一键导入平时分 |
| 3 | `--step 3` | 补录 2024-1 edu_grades（可选） | 演示”归档前”完整流程 |
| 4 | `--step 4` | edu_grades → edu_archives + 软删除 | 演示”提交锁定”归档流程 |

---

### 🐼 3. 组员 B (33%) - 学生端（✅ 按当前代码已落地）

**当前完成范围**：

- **学生端主界面**：`views/student/enrollment.py`
  - 页面：个人信息、选课系统、课表查询、成绩查询、考试查询、课程评教
  - 采用右侧竖直导航 + `QStackedWidget` 页面切换
  - 支持“刷新当前页面”（不重置数据库）
- **选课业务服务**：
  - `services/selection_service.py`（统一入口）
  - `services/student_selection_impl.py` + `services/student_selection_patch.py`（学生端选课实现与补丁挂载）
  - 已实现：选课、退课、候补、取消候补、退课后自动递补（事务内执行）
- **学生查询服务**：`services/student_service.py`
  - 个人信息、当前学期、可选/已选/候补课程、成绩、考试、评教、GPA、专业排名
  - 兼容旧库：`stu_exam_defer_req` 缺失时考试查询降级
- **冲突校验模块**：`services/validator.py`
  - 时间冲突、考试冲突、先修课校验逻辑可调用

**当前文件与接口（审核对照）**：

| 模块 | 文件路径 | 类名/接口名 | 当前实现要点 |
| :--- | :--- | :--- | :--- |
| 学生面板 | `views/student/enrollment.py` | `EnrollmentWidget` | 选课/退课/候补/取消候补、课程搜索、课表渲染、成绩检索与导出、考试缓考申请与取消、评教提交、密码修改 |
| 选课服务入口 | `services/selection_service.py` | `SelectionService` | `attempt_enroll`、`drop_course`、`cancel_waiting` |
| 学生选课实现 | `services/student_selection_impl.py` | 函数实现 | 事务化处理容量、状态、候补递补、异常转换 |
| 业务补丁挂载 | `services/student_selection_patch.py` | `apply_selection_patch` | 启动学生端时覆盖 `SelectionService` 的选课相关方法 |
| 学生查询服务 | `services/student_service.py` | `StudentService` | 个人信息/课程/成绩/考试/评教/GPA/排名/缓考接口 |
| 校验逻辑 | `services/validator.py` | `CourseValidator` | 先修课、时间冲突、考试冲突检测 |

**SQL 侧当前状态**：
- `sql/08_student_ext.sql`
  - 已实现并维护：`stu_info`、`stu_course_prereq`、`stu_exam_schedule`、`stu_exam_defer_req`、`stu_evaluation`
  - `proc_attempt_enroll`：以当前学期 `edu_teaching` 容量为准进行原子选课
  - `trigger_auto_fill_vacancy`：当前仅负责人数/状态同步，递补逻辑在服务层事务内完成
- `sql/09_student_demo_data.sql`
  - 学生端演示数据、候补队列场景、人数状态同步、`MA201`（高等数学）演示数据已补充

---

## 4. 目录结构规范
(建议在项目根目录下直接创建此结构)

```text
/my_db_project
│  main.py                # 启动入口
│  config.ini             # 数据库配置
│
├─core/                   # [组长] 核心底层
│      db_manager.py
│      logger.py
│      constants.py
│
├─services/               # 业务逻辑层 (按人分工)
│      auth_service.py    # [组长]
│      admin_service.py   # [组长]
│      grade_service.py   # [组员A]
│      teacher_service.py # [组员A]
│      student_service.py # [组员B]
│      selection_service.py # [组员B] 选课服务入口
│      student_selection_impl.py # [组员B]
│      student_selection_patch.py # [组员B]
│      validator.py       # [组员B] 选课冲突校验
│
├─views/                  # 主界面层 (当前代码实际路径)
│  ├─main_window.py       # [组长]
│  ├─login_dialog.py      # [组长]
│  └─student/
│      enrollment.py      # [组员B] 学生端主界面
│
├─ui/                     # 业务子页面组件
│  ├─admin/               # [组长]
│  └─teacher/             # [组员A]
│
└─sql/                    # SQL 脚本仓库
       01_init_schema.sql       # 建表总控
       02_views.sql             # [组员A] 视图定义
       03_procedures.sql        # 核心存储过程（通用）
       04_triggers.sql          # 核心触发器（通用）
       05_test_data.sql         # 基础测试数据
       06_teacher_ext.sql       # [组员A] 教师扩展
       07_teacher_demo_data.sql # [组员A] 教师演示数据
       08_student_ext.sql       # [组员B] 学生扩展（表/过程/触发器增强）
       09_student_demo_data.sql # [组员B] 学生演示数据
```

## 5. 合并与协作流程
1. **接口先行**: 组长先建立 `core/db_manager.py` 和 `services/` 下的空类（只写 `pass` 和函数名）。
2. **独立开发**: 大家 `pull` 下来后，各自去填空自己负责的 `.py` 文件，互不干扰。
3. **数据库同步**: 每个人维护自己的 SQL 片段，最后由组长按顺序执行 SQL 文件建立完整库。

---

## 6. 核心开发协定 (防冲突必读 - Critical Conventions)
*如果不遵守以下约定，代码合并时将100%出现变量名报错或逻辑冲突。*

### 6.1 "Rosetta Stone" 命名字典
为了防止你写 `uid` 而我写 `student_id`，统一规定核心命名：
*   **强制统一库**: 严禁混用，全员统一安装 **`PyQt6`** (`pip install PyQt6`)。
*   **主键命名**: 所有表的主键统一定义为 `id` (INT AUTO_INCREMENT)，不要用 `sid`, `cid`。
*   **外键命名**: `关联表单数_id` (e.g., `student_id`, `course_id`, `dept_id`)。
*   **通用字段**:
    *   创建时间: `created_at`
    *   逻辑删除位: `is_deleted` (0=正常, 1=已删)
    *   密码字段: `password_hash` (禁止存明文)
*   **对象传递**:
    *   `User` 对象必须包含属性: `id`, `username`, `role_id`, `real_name`。

### 6.2 数据交换标准 (Data Protocol) - 必读！
为了避免前端不知道后端传回来的是数组还是字典，规定如下：
1.  **DAO 层返回**: `DBManager.exec_query` 必须配置 cursor 为 **DictCursor**，永远返回 **字典列表** `[{'id':1, 'name':'xxx'}, ...]`，严禁返回元组。
2.  **软删除过滤**: 所有的 `SELECT` 查询，必须手写加上 `WHERE is_deleted = 0`。不要指望数据库自动过滤。

### 6.3 状态码枚举 (Magic Numbers)
禁止在代码里写死 `if role == 1`，必须在 `core/constants.py` 定义枚举：
```python
class RoleType:
    ADMIN = 1
    TEACHER = 2
    STUDENT = 3

class CourseStatus:
    OPEN = 'OPEN'      # 选课中
    CLOSED = 'CLOSED'  # 已截止
    FULL = 'FULL'      # 已满员
```

### 6.3 异常与交互规范
*   **Service 层**: 严禁 print 错误信息，严禁 return False。必须 **抛出异常**。
    *   ✅ `raise ValueError("课程时间冲突")`
    *   ❌ `return False` (前端不知道是满了还是冲突了)
*   **View 层**: 只有 View 层允许调用 `QMessageBox.warning` 捕获 Service 抛出的异常并展示。

### 6.4 全局单例与异常定义 (Context Management)
为了统一数据流与错误处理，避免代码冲突：
1. **统一异常类**: 在 `core/exceptions.py` 定义业务异常基类 `BusinessError`，所有 Service 抛出的自定义错误需继承自它。
2. **Session 维护**: 登录成功后，组长负责将用户信息写入全局单例 `Session.current_user`。
    *   **组员调用**: `current_uid = Session.current_user.id` 即可获取当前登录者。
    *   **注销说明**: 必须调用 `Session.clear()` 清空用户信息，防止越权。

## 6.5 UI/UX 增强实现 (Wave 1-2 已完成)

### Task 1: 用户管理界面优化与按钮重组 ✅

**问题诊断**:
- 原有虚拟按钮"软删除示例ID=1"无实际功能，占用界面空间
- 按钮排列混乱，缺乏逻辑分组，影响用户体验

**解决方案**:
- **移除虚拟按钮**: 删除 `btn_soft_delete` 声明及其占位符（行142），合并删除逻辑到 `soft_delete_selected_user()` 方法
- **按钮重新分组**: 将剩余6个按钮组织为3个逻辑分组：
  - **Group 1 (创建与导入)**: `btn_create_user`, `btn_import`
  - **Group 2 (管理操作)**: `btn_refresh`, `btn_update`, `btn_soft_delete_selected`
  - **Group 3 (系统操作)**: `btn_verify_audit`
- **布局改进**: 使用 `QHBoxLayout` 分组并添加分隔符 (`QFrame` + `setFrameShape(QFrame.VLine)`)，使按钮分组视觉清晰

**文件修改**:
- `views/admin/user_mgmt.py` (142行删除虚拟按钮，130-160行重组按钮布局)

**验证结果**: ✅ LSP diagnostics: 0 errors, 按钮排列整洁美观

---

### Task 2: 课程/讲师选择增强 (下拉框搜索) ✅

**问题诊断**:
- 原有 `QComboBox` 仅支持下拉选择，大数据量情况下用户需要滚动翻找
- 课程代码、讲师名字等信息难以快速定位

**解决方案**:
- **QCompleter集成**: 为所有课程/讲师下拉框添加 `QCompleter` 支持
  - 启用编辑模式 (`setEditable(True)`)
  - 配置Completer的排序模式为 `PopupCompletion`（即时弹窗显示匹配项）
  - 设置大小写不敏感 (`CaseSensitivity.CaseInsensitive`)
- **保持数据完整性**: `currentData()` 继续返回正确的数据对象，确保后续业务逻辑不受影响

**实现细节** (在 `_build_ui` 中调用):
```python
def _setup_completer(self, combo: QComboBox) -> None:
    """为下拉框启用QCompleter搜索功能"""
    combo.setEditable(True)
    completer = QCompleter(combo.model(), combo)
    completer.setCompletionMode(QCompleter.CompletionMode.PopupCompletion)
    completer.setCaseSensitivity(Qt.CaseSensitivity.CaseInsensitive)
    combo.setCompleter(completer)
```

**应用范围** (4处下拉框):
- `combo_new_course` (新建教学安排 - 课程)
- `combo_new_teacher` (新建教学安排 - 讲师)
- `combo_edit_course` (编辑教学安排 - 课程)
- `combo_edit_teacher` (编辑教学安排 - 讲师)

**文件修改**:
- `views/admin/teaching_mgmt.py` (_setup_completer 方法 + 4处调用)

**验证结果**: ✅ LSP diagnostics: 0 errors, 用户可在输入框即时过滤课程代码/讲师名字

---

### Task 3: 所有管理模块表格增强 (搜索/排序/筛选) ✅

**问题诊断**:
- 所有数据表都使用 `QTableWidget` 仅做原始展示，无搜索/排序/筛选功能
- 大数据量情况下用户难以快速找到需要的记录
- 表格显示框过小，数据密集，体验不佳

**解决方案**:
- **搜索栏**: 在每个表上方添加 `QLineEdit`，支持全列实时搜索（大小写不敏感，模糊匹配）
- **排序**: 启用表格列头排序 (`setSortingEnabled(True)`)，用户点击列头可按升序/降序排列
- **筛选**: 搜索时动态隐藏不匹配的行（`setRowHidden(row, bool)`），保持表格结构完整

**实现模式** (所有4个管理模块统一):

搜索栏UI:
```python
self.search_input = QLineEdit()
self.search_input.setPlaceholderText(f"搜索{module_name}...")
self.search_input.textChanged.connect(self._filter_xxx)
layout.addWidget(self.search_input)
layout.addWidget(self.table)
```

过滤逻辑:
```python
def _filter_xxx(self, text: str) -> None:
    """全列搜索与行隐藏"""
    search_text = text.strip().lower()
    for row in range(self.table.rowCount()):
        if not search_text:
            self.table.setRowHidden(row, False)  # 搜索为空 -> 显示所有行
        else:
            # 任意列匹配即视为命中
            match_found = any(
                search_text in (self.table.item(row, col)?.text() or "").lower()
                for col in range(self.table.columnCount())
            )
            self.table.setRowHidden(row, not match_found)
```

**应用范围** (4个模块):
1. **部门管理** (`dept_mgmt.py`) - 4列表格
   - 搜索栏: "搜索院系..."
   - 过滤方法: `_filter_depts(text)`
   
2. **专业管理** (`major_mgmt.py`) - 5列表格
   - 搜索栏: "搜索专业..."
   - 过滤方法: `_filter_majors(text)`
   
3. **课程管理** (`course_mgmt.py`) - 7列表格
   - 搜索栏: "搜索课程..."
   - 过滤方法: `_filter_courses(text)`
   
4. **教学安排管理** (`teaching_mgmt.py`) - 10列表格
   - 搜索栏: "搜索授课安排..."
   - 过滤方法: `_filter_teachings(text)`

**文件修改**:
- `views/admin/dept_mgmt.py` (搜索栏UI + _filter_depts方法)
- `views/admin/major_mgmt.py` (搜索栏UI + _filter_majors方法)
- `views/admin/course_mgmt.py` (搜索栏UI + _filter_courses方法)
- `views/admin/teaching_mgmt.py` (搜索栏UI + _filter_teachings方法)

**验证结果**: ✅ LSP diagnostics: 所有4个文件0 errors, 搜索与排序功能正常运作

---

### Task 4: 响应式设计实现 (自适应窗口缩放) ✅

**问题诊断**:
- 程序所有尺寸均使用硬编码像素值（字体18-20px、按钮高70px、宽度180px、间距24px等）
- 窗口缩放时界面内容不跟随缩放，导致窗口变大时大量空白，变小时内容溢出
- 用户体验极差，尤其在多分辨率场景下

**根本原因**:
1. 样式表全量使用绝对像素单位
2. 组件宽高使用 `setMinimumWidth()`, `setFixedHeight()` 等绝对值
3. 布局边距使用硬编码数字
4. Splitter分割比例使用绝对像素值而非相对比例

**解决方案** (3层改造):

#### Layer 1: 样式表优化 (`ui/assets/theme.qss`)
- 减少过度精细化的像素定义
- 字体大小：`20px` → `18px` (标题), `10px` → `9px` (辅助文本) - 降低过度放大的视觉效应
- 导航栏最小宽度：`200px` → `140px` (适应侧栏自适应)

#### Layer 2: 主窗口布局改造 (`views/main_window.py`)
```python
# 移除固定高度，使用SizePolicy适应
header.setSizePolicy(QSizePolicy.Policy.Expanding, QSizePolicy.Policy.Minimum)

# Splitter使用相对比例而非绝对像素
self.splitter.setStretchFactor(0, 1)   # 左侧导航占1份
self.splitter.setStretchFactor(1, 3)   # 右侧内容占3份

# 减少边距
layout.setContentsMargins(12, 12, 12, 12)  # 原: 20, 12, 20, 20
```

#### Layer 3: 所有管理/教师模块组件改造
**移除的硬编码**:
- `setMinimumWidth(180)` - 让ComboBox自适应宽度
- `setFixedHeight(60)` - 让Button自适应高度
- `setFixedSize(width, height)` - 统一使用SizePolicy

**添加的自适应策略**:
```python
# 对需要占用空间的组件
widget.setSizePolicy(QSizePolicy.Policy.Expanding, QSizePolicy.Policy.Minimum)

# 保持布局一致
layout.setContentsMargins(10, 8, 10, 8)  # 统一边距（原混乱的24, 16等）
layout.setSpacing(8)  # 统一间距
```

**改造范围** (全局):
- `views/main_window.py` - 主窗口Splitter与Header
- `views/teacher/` - 教师模块所有窗口
- `views/admin/user_mgmt.py` - 用户管理
- `views/admin/teaching_mgmt.py` - 教学安排
- `views/admin/dept_mgmt.py` - 部门管理
- `views/admin/major_mgmt.py` - 专业管理
- `views/admin/course_mgmt.py` - 课程管理
- `ui/assets/theme.qss` - 全局样式表

**关键改变**:
- ✅ Table宽度不再限制，自动填满容器宽度
- ✅ Button/Input高度自适应（基于内容与SizePolicy）
- ✅ 字体大小统一降低，避免过度放大
- ✅ 所有间距与边距使用相对比例，窗口缩放时保持比例关系

**文件修改**:
- `views/main_window.py` (Splitter与Header)
- `ui/assets/theme.qss` (字体与最小宽度)
- `ui/admin/user_mgmt.py`, `teaching_mgmt.py`, `dept_mgmt.py`, `major_mgmt.py`, `course_mgmt.py`
- `views/teacher/` (所有教师模块)

**验证结果**: ✅ LSP diagnostics: 所有修改文件0 errors, 窗口任意缩放时内容按比例自适应，无裁剪无溢出

---

## 7. 高级合并技术规约 (Merger Optimization)

### 7.1 数据库连接与多线程
*   **连接池 (Core)**: `DBManager` 必须初始化一个全局连接池。
*   **非阻塞调用**: UI 层在执行耗时 SQL（如复杂报表）时，必须使用 `QThread` 或 `run_in_executor`，严禁在主线程直接阻塞等待 `exec_query` 结果。

### 7.2 模块化导入 (Absolute Imports)
*   **统一入口**: 所有代码以根目录 `/my_db_project` 为基准。
*   **强制语法**: 严禁使用 `from ..core import ...` 或 `import db_manager`。必须统一使用 `from core.db_manager import DBManager`。

### 7.3 数据库版本同步流程
*   **Schema 变更**: 若需修改他人负责的表或增加全局字段，必须由发起人在群内同步，并更新 `01_init_schema.sql`。
*   **依赖顺序**: 启动项目前，确保 SQL 执行顺序为：`Init (表) -> Views (视图) -> Functions/Procedures (函数/过程) -> Triggers (触发器) -> Data (数据)`。

### 7.4 软删除底层逻辑
*   **DAO 封装**: 鼓励组长在 `DBManager.exec_query` 中根据参数自动拼装 `AND is_deleted = 0`，以减少手动维护漏掉的风险。

