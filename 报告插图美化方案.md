# 报告插图美化方案（来源建议 + Prompt + 代码）

目标：在保证真实性的前提下，把报告中的图做得更专业、更统一、更美观。

## 一、图片来源总策略（美观第一）

- 功能证据图（管理员端、教师端、学生端）：必须使用真实系统截图。
- 结构图（架构图、关系图、E-R 图）：优先代码生成（可复现、风格统一）。
- 装饰图（封面背景、章节分隔视觉）：可用大模型生成，不参与功能证明。

建议分辨率：
- 报告插图：宽度 2200px 以上。
- 页面截图：16:9（1920x1080）或 A4 宽版（2480x1754）。

统一视觉风格建议：
- 主色：#0E3A5D（深海蓝）、#1FA187（青绿）、#F4B942（金橙）
- 背景：#F6F8FB
- 标题字体：思源黑体/阿里巴巴普惠体

## 二、每张图的推荐来源

1) 截图占位-1（系统架构图）
- 推荐来源：代码软件（Mermaid）
- 原因：结构清晰、易修改、导出矢量图后可二次美化

2) 截图占位-2（数据库 E-R 图，21表）
- 推荐来源：代码软件（Graphviz 或 MySQL Workbench 反向工程）
- 原因：关系密集，代码生成比手绘更准确

3) 截图占位-3（核心关系图）
- 推荐来源：代码软件（Graphviz）
- 原因：适合做“答辩一眼看懂”的海报式关系图

4) 截图占位-4/5/6（功能界面）
- 推荐来源：真实系统截图 + 轻后期
- 原因：真实性必须保留；仅做清晰度和色彩统一提升

## 三、可直接使用的代码

### A. Mermaid 架构图代码（对应截图占位-1）

将以下内容粘贴到 Mermaid 渲染器后导出 SVG：

graph LR
    A[登录认证层\nLoginDialog + AuthService] --> B[会话与路由层\nSession + MainWindow]
    B --> C[管理员端\n用户/院系/专业/课程/教学安排/审批]
    B --> D[教师端\n成绩录入/分析/归档/调课]
    B --> E[学生端\n选课/候补/考试/评教]

    C --> F[Service 层\nadmin_service/auth_service]
    D --> G[Service 层\ngrade_service/teacher_service]
    E --> H[Service 层\nstudent_service/selection_service]

    F --> I[DBManager\n连接池/事务/参数化查询]
    G --> I
    H --> I

    I --> J[(MySQL 8.0\n21 tables)]
    J --> K[视图 5个]
    J --> L[过程 3个]
    J --> M[触发器 4个]

### B. Graphviz 核心关系图 Python 代码（对应截图占位-3）

保存为 make_core_graph.py 后执行：python make_core_graph.py

from graphviz import Digraph


g = Digraph("core_relations", format="svg")
g.attr(rankdir="LR", bgcolor="#F6F8FB", splines="spline")
g.attr("node", shape="box", style="rounded,filled", fontname="Microsoft YaHei", fontsize="11")

g.node("sys_users", "sys_users\n用户", fillcolor="#CFE8FF")
g.node("edu_courses", "edu_courses\n课程", fillcolor="#D7F5E8")
g.node("edu_teaching", "edu_teaching\n授课安排", fillcolor="#FFF2CC")
g.node("stu_selection", "stu_selection\n选课记录", fillcolor="#FDE2E2")
g.node("stu_waiting", "stu_waiting_list\n候补队列", fillcolor="#FDE2E2")
g.node("edu_grades", "edu_grades\n成绩", fillcolor="#EADCF8")
g.node("edu_archives", "edu_archives\n归档", fillcolor="#EADCF8")
g.node("sys_audit", "sys_audit_logs\n审计日志", fillcolor="#FFE8C2")

g.edge("sys_users", "edu_teaching", label="teacher_id")
g.edge("edu_courses", "edu_teaching", label="course_id")
g.edge("sys_users", "stu_selection", label="student_id")
g.edge("edu_teaching", "stu_selection", label="teaching_id")
g.edge("sys_users", "stu_waiting", label="student_id")
g.edge("edu_teaching", "stu_waiting", label="teaching_id")
g.edge("sys_users", "edu_grades", label="student_id")
g.edge("edu_teaching", "edu_grades", label="teaching_id")
g.edge("edu_grades", "edu_archives", label="original_grade_id")
g.edge("sys_users", "sys_audit", label="user_id")

g.render("core_relations", cleanup=True)
print("生成完成: core_relations.svg")

### C. Graphviz 21表分域关系图代码（对应截图占位-2）

保存为 make_er_21tables.py 后执行：python make_er_21tables.py

from graphviz import Digraph


g = Digraph("er_21_tables", format="svg")
g.attr(rankdir="LR", bgcolor="#F6F8FB", splines="polyline", nodesep="0.35", ranksep="0.6")
g.attr("node", shape="record", style="rounded,filled", fontname="Microsoft YaHei", fontsize="10")

# 系统域
sys_tables = [
    "sys_roles", "sys_permissions", "sys_role_permissions", "sys_users",
    "sys_audit_logs", "sys_config", "base_dept", "base_major"
]
for t in sys_tables:
    g.node(t, f"{{{t}}}", fillcolor="#CFE8FF")

# 教学域
edu_tables = [
    "edu_courses", "edu_teaching", "edu_grades", "edu_archives",
    "edu_daily_records", "edu_schedule_change_req"
]
for t in edu_tables:
    g.node(t, f"{{{t}}}", fillcolor="#D7F5E8")

# 学生域
stu_tables = [
    "stu_info", "stu_selection", "stu_waiting_list", "stu_course_prereq",
    "stu_exam_schedule", "stu_exam_defer_req", "stu_evaluation"
]
for t in stu_tables:
    g.node(t, f"{{{t}}}", fillcolor="#FFE5D5")

# 核心关系（可按需要继续补）
g.edge("sys_roles", "sys_users")
g.edge("base_dept", "base_major")
g.edge("base_dept", "edu_courses")
g.edge("base_major", "sys_users")
g.edge("sys_users", "sys_audit_logs")
g.edge("edu_courses", "edu_teaching")
g.edge("sys_users", "edu_teaching")
g.edge("edu_teaching", "edu_grades")
g.edge("sys_users", "edu_grades")
g.edge("sys_users", "stu_selection")
g.edge("edu_teaching", "stu_selection")
g.edge("sys_users", "stu_waiting_list")
g.edge("edu_teaching", "stu_waiting_list")
g.edge("sys_users", "stu_info")
g.edge("edu_courses", "stu_exam_schedule")
g.edge("sys_users", "stu_exam_defer_req")
g.edge("edu_courses", "stu_exam_defer_req")
g.edge("sys_users", "stu_evaluation")
g.edge("edu_courses", "stu_evaluation")
g.edge("edu_teaching", "edu_daily_records")
g.edge("sys_users", "edu_daily_records")
g.edge("edu_teaching", "edu_schedule_change_req")
g.edge("sys_users", "edu_schedule_change_req")

g.render("er_21_tables", cleanup=True)
print("生成完成: er_21_tables.svg")

## 四、可直接使用的 Prompt（大模型）

### 1) 封面装饰图 Prompt（建议用于章节封面，不做功能证据）

中文 Prompt：
高质量学术海报风，主题为数据库教学管理系统，三层结构可视化元素（表示层、业务层、数据层），包含数据表连接线、SQL符号、窗口函数曲线图抽象元素，主色深蓝与青绿，辅色金橙，画面干净、专业、现代、无人物、无文字、16:9、超清。

English Prompt:
Academic poster style illustration for a university database teaching management system, visualizing three layers (presentation, service, data), abstract elements of relational tables, SQL symbols, and analytic curves, deep blue and teal primary palette with warm gold accent, clean modern composition, no people, no text, 16:9, ultra high resolution.

### 2) 架构图美化 Prompt（图像后期，保真增强）

用于图像到图像（i2i）时：
保持原有节点文字和连线含义不变，不新增节点，不删除节点。仅优化视觉：统一配色为深蓝/青绿/金橙，提升线条清晰度，增加轻微阴影和留白，提升对齐与排版平衡，整体呈现高质量学术答辩海报风格。

### 3) 界面截图后期 Prompt（管理员/教师/学生界面）

保持界面结构、文字、数据内容完全不变；仅做高清修复、噪点去除、对比度和白平衡优化；保留真实软件截图质感，不做二次设计，不替换字体，不改变按钮位置。

## 五、截图实操建议（真实性 + 美观）

- 先在系统中准备“可读数据量”的页面，再截图。
- 建议工具：系统截图 + 简单标注（箭头/编号）。
- 每张功能图保留窗口标题和时间，增强真实性。
- 图片统一加白边和标题条，整份报告视觉会更整齐。

推荐截图顺序：
1. 管理员首页与菜单全景
2. 教师端成绩分析图
3. 学生端选课三表
4. 审批页面（调课/缓考）
5. 审计日志查询结果
